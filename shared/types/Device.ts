type Device = {
    domain: string;
    macAddr: string;
    hostName: string;
    ipAddress: string;
    onlineStatus: string;
    connectionType: string;
    ssid: string;
};

export default Device;